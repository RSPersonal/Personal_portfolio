const portfolioTotalProfit = JSON.parse(document.getElementById("portfolioOveralReturn").textContent);

var idArray = ['portfolioOveralReturn'];

idArray.forEach(item => {
    console.log(item)

    // const itemValue = JSON.parse(document.getElementById(`$item`).textContent);
    // if (itemValue < 0) {
        // const element = document.getElementById(`"$item"`);
        // const elementText = document.getElementById("overalReturnDivText");
        // element.style.backgroundColor='red';
    // }
})